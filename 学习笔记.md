[English](learning-notes.md) &nbsp;|&nbsp; **中文**

<br>

<div align="center">

# 我的 Git 学习笔记

### *从零到 Git/GitHub — 两天进化日志*

<br>

> 师傅：**Momo** 🍑 &nbsp;·&nbsp; 学员：**andyttc05**

<br>

[时间线](#-学习时间线) &nbsp;·&nbsp;
[安装 Git](#-安装-git) &nbsp;·&nbsp;
[第一次 Push](#first-push-cn) &nbsp;·&nbsp;
[GitHub Desktop](#github-desktop-cn) &nbsp;·&nbsp;
[Pages 建站](#github-pages-cn) &nbsp;·&nbsp;
[基础概念](#-核心概念) &nbsp;·&nbsp;
[日常工作流](#-日常三连) &nbsp;·&nbsp;
[常见坑](#-常踩的坑) &nbsp;·&nbsp;
[配置](#-pycharm-配置) &nbsp;·&nbsp;
[下一步](#-下一步)

</div>

<br>
<br>

---

## 🎓 学习时间线

| 时间 | 里程碑 |
| :--- | :--- |
| **8/1 &middot; 中午** | 注册 GitHub 账号 + 第一次 commit |
| **8/1 &middot; 下午** | 第一次 push 到 GitHub（终端 + token） |
| **8/1 &middot; 晚上** | 安装 GitHub Desktop &middot; 独立完成 commit + push |
| **8/1 &middot; 深夜** | 学 `.gitignore` + PyCharm 集成 |
| **8/2 &middot; 凌晨** | 仓库移到 `~/Documents` + PyCharm & GitHub Desktop 完全重置 |
| **8/2 &middot; 白天** | 从零建了中英双语的 GitHub Pages 个人主页 → [andyttc05.github.io](https://andyttc05.github.io) |
| **8/2 &middot; 下午** | 仓库双语化（README + 笔记 中英各一版） + 全面设计优化 |

<br>

---

## 💻 安装 Git

> 💡 装 Git 是第一步——没有它，commit、push、GitHub Desktop 都无从谈起。

### macOS（我的环境）

```bash
# 推荐用 Homebrew 装（一行搞定）
brew install git
```

装完验证：

```bash
git --version
# → git version 2.50.1
```

有版本号输出 = 装好了。

### Windows

去 [git-scm.com](https://git-scm.com) 下载安装包，一路点「下一步」。装完在「Git Bash」里敲 `git --version` 验证。

### 装完第一件事：自报家门

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

> ⚠️ 邮箱**必须和 GitHub 注册邮箱一致**，否则 commit 记录不会挂到你的 GitHub 头像上。

<br>

---

<a id="first-push-cn"></a>

## 🚀 第一次 Push 全流程

> 💡 这个流程我第一天走了很多弯路——写下来给后来的自己（和任何从零开始的人）。

### ① 注册 GitHub 账号

1. 打开 [github.com](https://github.com) → 点 **Sign up**
2. 填邮箱、密码、用户名（建议用英文，以后就是你的开发者身份）
3. 验证邮箱 → 完成

### ② 在 GitHub 网页新建仓库

1. 登录后点右上角 **+** → **New repository**
2. **Repository name** 填项目名（如 `my-first-repo`）
3. 选 **Public**（公开）或 **Private**（私有）
4. ⚠️ **不要勾** "Add a README file"（后面本地会建）
5. 点 **Create repository** → 记下页面显示的网址：
   ```
   https://github.com/你的用户名/仓库名.git
   ```

### ③ 本地初始化 + 连接云端

```bash
cd 你的项目文件夹
git init                                    # 初始化 Git 仓库
git add .                                   # 暂存所有文件
git commit -m "first commit"                # 第一次提交
git remote add origin 刚才复制的网址          # 连接 GitHub
git push -u origin main                     # 上传
```

### ④ 关于 Token（认证）

2021 年起 GitHub 不允许用密码 push，需要 **Personal Access Token**：

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token → 勾选 `repo` 权限 → 生成
3. push 时用户名填你的 GitHub 用户名，密码填 **这串 token**（不是登录密码）

> 💡 装了 GitHub Desktop 之后就不用管 token 了——浏览器授权一次，永久免密。

### ⑤ 去 GitHub 网页刷新

打开 `https://github.com/你的用户名/仓库名` —— 代码已经在云端了 🎉

<br>

---

<a id="github-desktop-cn"></a>

## 🖥 GitHub Desktop 安装与使用

> 💡 这是我的主力工具——全程点按钮，不碰终端。

### 安装

去 [desktop.github.com](https://desktop.github.com) 下载 → 双击 dmg → 拖进 Applications → 完成。

### 首次登录

打开 GitHub Desktop → **Sign in to GitHub.com** → 浏览器弹窗 → 点 **Authorize** → 自动回到桌面版。**不需要 token**，不需要密码。

### 日常三步

1. **改文件**（用 PyCharm / 记事本 / 任何编辑器）
2. 切回 GitHub Desktop → 左侧 **Changes** 自动列出改动
3. 填 **Summary** → 点 **Commit to main** → 顶部出现 **Push origin** → 点

> 🎯 不用 `git add`、不用 `git push`——Commits 里所有的改动自动暂存，点 Push 一键上传。

### 添加已有仓库

**File → Add local repository** → 选文件夹 → Add。仓库立刻出现在左侧列表，以后点一下就能切。

### 与终端的关系

两者互不冲突。你在终端 `git commit` 了，GitHub Desktop 照样能看到。反过来也一样。

<br>

---

<a id="github-pages-cn"></a>

## 🌐 GitHub Pages 建站笔记

> 💡 这是我学的第一个"非 Git"技能，但全程靠 Git 管理。

### 做了什么

1. 建仓库 `andyttc05.github.io`（GitHub 自动识别为 Pages 站点）
2. 写纯 HTML + CSS 个人主页（editorial 杂志风）
3. 拆成 `index.html`（英文）+ `zh.html`（中文），点按钮跳转
4. 加 Pull Quote、渐入动效、交替背景、响应式布局
5. 修 Safari hover 颜色 bug

### 学到的

| 概念 | 要点 |
| :--- | :--- |
| GitHub Pages | 仓库名 = `用户名.github.io` → 自动部署，免费域名 |
| 静态网站 | HTML + CSS → 浏览器直接打开，无需服务器 |
| 双语设计 | 双文件 + 卡片式语言切换 → 页面刷新跳转 |
| 设计迭代 | Pull Quote / 动效 / 交替背景 / 响应式 → 在 PyCharm 改代码，GitHub Desktop push |

<br>

---

## 🎯 核心概念

### Git 是什么
给项目拍照存档的工具 — 每次 commit 都是一张可以回看的照片。

### 四区域流转

```
工作区            我修改文件的地方
  ↓  git add
暂存区            排队等待提交
  ↓  git commit
本地仓库          保存快照
  ↓  git push
GitHub（云端）    上传到网上
```

### 我的账号

| 项目 | 内容 |
| :--- | :--- |
| GitHub | [andyttc05](https://github.com/andyttc05) |
| 邮箱 | andyttc2463@gmail.com |
| 配置路径 | `~/.config/git/config`（XDG 路径，沙箱安全） |

<br>

---

## ⚡ 日常三连

```bash
git add .                         # 暂存所有改动
git commit -m "这次做了什么"        # 拍照存档
git push                          # 上传到 GitHub
```

> 💡 **不用终端也行**：上面三行在 **GitHub Desktop** 里点按钮就能完成。

<br>

---

## 🔗 PyCharm + GitHub Desktop 协同

### 自动同步的真相

它们共享**同一个文件夹**，不需要手动同步：

```
PyCharm 打开 ~/Documents/my-first-repo
              ↓ 保存文件
     my-first-repo 文件夹
              ↓ 自动检测
  GitHub Desktop 显示改动 ✓
```

**前提条件：**
- PyCharm 打开的是 Documents 的 my-first-repo
- GitHub Desktop 添加的也是 Documents 的 my-first-repo

### 操作步骤

1. PyCharm 里写代码（切窗口自动保存）
2. 切到 GitHub Desktop → 看到 **Changes**
3. 写 **Summary** → 点 **Commit to main**
4. 顶部出现 **Push origin** → 点击 → 完成

<br>

---

## ⚠️ 常踩的坑

<details open>
<summary><strong>坑 1：「No local changes」是好事不是 bug</strong></summary>
<br>

commit + push 之后工作区干净了。GitHub Desktop 显示「No local changes」意思是 *"全部提交完，没有遗留"*。

</details>

<details open>
<summary><strong>坑 2：.gitignore 管不了已 add 的文件</strong></summary>
<br>

> `.gitignore` 只对**未跟踪**的文件生效。已经 `git add` 过的文件免疫。

要脱钩：

```bash
git rm --cached -r 文件路径   # 从暂存区拿下来，本地文件保留
```

</details>

<details open>
<summary><strong>坑 3：仓库移动不会丢</strong></summary>
<br>

Git 仓库 = 文件夹里带 `.git` 隐藏目录。**自带迁移能力。**

- 整个文件夹拖到任何地方 — 代码、历史、远程连接全部跟着走
- 唯一要做的事：GitHub Desktop → **File → Add local repository** → 选新位置

</details>

<br>

---

## 🐍 PyCharm 配置

| 设置项 | 值 | 位置 |
| :--- | :--- | :--- |
| 主题 | **Light** | Settings → Appearance |
| 字体大小 | **15** | Settings → Editor → Font |
| 自动保存 | **切换窗口时** | Settings → System Settings |

<br>

---

## 📂 工具链

| 工具 | 状态 |
| :--- | :---: |
| **Git** `v2.50.1` | ✅ |
| **GitHub Desktop** | ✅ |
| **PyCharm** `2026.1`（Light / 字体 15） | ✅ |
| **GitHub Pages** → [andyttc05.github.io](https://andyttc05.github.io) | ✅ |
| **Claude Code** | ⬜ |

### 关键路径

| 项目 | 路径 |
| :--- | :--- |
| 仓库 | `~/Documents/my-first-repo` |
| 远程 | `https://github.com/andyttc05/my-first-repo` |
| Git 配置 | `~/.config/git/config` |

<br>

---

## 🔄 如何重置工具

`mv` 配置目录到 `/tmp/` = 恢复出厂：

```bash
# PyCharm
mv ~/Library/Application\ Support/JetBrains/PyCharm2026.1 /tmp/removed-pycharm

# GitHub Desktop
mv ~/Library/Application\ Support/GitHub\ Desktop /tmp/removed-github-desktop
```

> 📦 旧配置留在 `/tmp/`，需要恢复随时找。

<br>

---

## 🎯 下一步

- [x] ~~GitHub Pages~~ → [andyttc05.github.io](https://andyttc05.github.io)（中英双语，editorial 设计）
- [x] ~~中英双语~~ README + 笔记全部双语化
- [x] ~~仓库设计优化~~ Shields.io 替换为 editorial 元数据，加导航栏 + HTML 表格 + 折叠面板
- [ ] 装 Claude Code 命令行（需 Pro/Max 订阅）
- [ ] 学分支（`git branch`、`git merge`）
- [ ] 做一个真正的 Python 项目

<br>

---

<div align="center">

> 🍑 *"学习就像拍照片 — 每 commit 一次，就给自己留一张可以回看的快照。"*
> — Momo

</div>
